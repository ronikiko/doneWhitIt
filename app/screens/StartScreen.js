import React from 'react'
import Spinner from 'react-native-loading-spinner-overlay'

const StartScreen = () => {
	return <Spinner visible={true} textContent="Please Wait..." />
}

export default StartScreen
